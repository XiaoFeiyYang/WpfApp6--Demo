<Window x:Class="WpfApp6_餐馆点餐系统Demo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp6_餐馆点餐系统Demo"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Border BorderBrush="LightYellow" BorderThickness="3" CornerRadius="6">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <StackPanel>
                <TextBlock Text="点我一下"/>
                <TextBlock Text="点我一下"/>
                <TextBlock Text="点我一下"/>
            </StackPanel>
            <DataGrid Grid.Row="1">
                <DataGrid.Columns/>
            </DataGrid>
            <StackPanel Grid.Row="2" Orientation="Horizontal">
                <TextBlock Text="点我一下"/>
                <TextBox IsReadOnly="True"/>
                <Button Content="点菜" HorizontalAlignment="Right"/>
            </StackPanel>
        </Grid>
    </Border>
   
</Window>
